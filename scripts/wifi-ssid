 #!/usr/bin/env bash
SSID=$(powershell.exe -Command "(Get-NetConnectionProfile).Name" | tr -d '\r')
if [[ -z "$SSID" ]]; then
    echo "No WiFi connection found."
else
    echo "Connected to: $SSID"
fi
