#!/usr/bin/env bash

gen_pass() {
  all="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()-_=+[]{};:,.<>?"
  pass=""
  for ((i=0; i<$1; i++)); do
    idx=$(( RANDOM % ${#all} ))
    pass+="${all:idx:1}"
  done
  echo "$pass"
}

gen_pass $1
